[package]
name = "webai-core"
version = "0.0.1"
edition = "2021"
description = "Core types and errors for WebAI-MCP"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
schemars = "0.8"
