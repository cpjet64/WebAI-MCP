---`ntitle: EXECUTION PLAN`nauthor: autonomous standardizer`nversion: 1.0.0`nscope: project standardization`nstack:`n  - workspace: Node.js (npm/pnpm)`n  - tooling: cargo, jest, chrome extension tooling`ncreated: 2026-03-01`nstatus: active`n---`n`n# Execution Plan`n`n## 1) Purpose`nProduce a stable, reusable execution scaffold for this repository by creating/updating:`n`n- `MASTER-CHECKLIST.md``n- `EXECUTION-PLAN.md``n`nKeep docs minimal, clear, and actionable.`n`n## 2) Inputs and Constraints`n`n- Scope: `C:\\Dev\\repos\\active\\WebAI-MCP``n- Root instructions: `AGENTS.md`, `CLAUDE.md`, and any repository-specific docs in this tree.`n- This repository is a multi-package Node workspace (`package.json` defines workspaces: `webai-mcp`, `webai-server`) with additional Rust-oriented CI tasks in `Justfile`.`n- Existing quality controls include `npm run build:all` (per repo instructions), workspace builds/tests, and a `ci-fast`/`ci-deep` path in `Justfile`.`n`n## 3) Plan of Work`n`n### Phase A — Discovery and Baseline`n`n1. Verify project root and branch.`n2. Record baseline `HEAD` in `.agent-state/last-head.txt`.`n3. Enumerate critical docs (`README.md`, this AGENTS file, `Justfile`, and any `SPEC*.md`).`n4. Detect stack shape using manifest files (`package.json`, `webai-mcp/package.json`, `webai-server/package.json`).`n`n### Phase B — Canonical Standards Creation`n`n1. Generate/update `MASTER-CHECKLIST.md` as a reusable checklist template.`n2. Generate/update `EXECUTION-PLAN.md` with:`n   - stack-specific build/test commands,`n   - responsibilities and agent handoff guidance,`n   - completion criteria and sequencing.`n`n### Phase C — Tracking and Reporting`n`n1. Create `docs/standardization-report.md` and append progress entries with timestamps.`n2. Record completion status and changed files.`n`n## 4) Validation Commands`n`n- `npm run build:all``n- `git status --short` (post-change check)`n- `git diff --check` (for whitespace/format noise)`n`n> Notes: This pass focuses on docs-only standardization; broader build/test execution should be run by implementation owners.`n`n## 5) Completion Criteria`n`n- `MASTER-CHECKLIST.md` and `EXECUTION-PLAN.md` exist with clear phase-based structure.`n- `docs/standardization-report.md` contains dated progress log.`n- Changes committed with conventional commit message including `[s-project-standardizer]`.`n